import pygame
import config as cg

class Renderer:
    def draw(self, grid):
        ...
        # basically we just need to graph 3 separate views of our cube 
        # and could the number of active cells in the slice to choose
        # the color value for our gradient
    